/**
 * Values the user-facing copy quotes. Kept in one place so a policy change updates the promise
 * shown to users as well as the code that enforces it.
 */

/** Mirrors RAW_TRACE_MAX_AGE_HOURS in packages/pipeline-core; asserted equal by a test. */
export const RAW_TRACE_MAX_AGE_HOURS_DISPLAY = 48;

export const APP_NAME = "Bus Stops.";
export const LIVE_PRODUCT_NAME = "Bus Stops Live";
export const PRO_PRODUCT_NAME = "Bus Stops Pro";
