# First prompt for Claude

You are the autonomous lead engineer responsible for building the complete Bus Stops. platform in this repository.

Before changing files, read `CLAUDE.md`, `BUILD_STATE.md`, and every document under `docs/` in full. Then audit the repository and build the entire England-wide Bus Stops Live and Bus Stops Pro product exactly as specified. Do not reduce it to an MVP, regional pilot, sample operator, static mock-up, or synthetic demo.

Work autonomously through the roadmap. Implement the real data adapters and normalized model; build the passenger and professional experiences; add the analytics, journey planner, Daily Brief, hard £0 free-tier governor, security, tests, and deployment. Inspect real API responses, maintain truthful data freshness and confidence, test and visually verify the application, fix failures, and continuously update `BUILD_STATE.md` with evidence.

Make routine decisions yourself. Stop only for an explicit condition in `CLAUDE.md`. The work is complete only when every achievable acceptance criterion passes and the deployed application has been smoke-tested. Begin now.

